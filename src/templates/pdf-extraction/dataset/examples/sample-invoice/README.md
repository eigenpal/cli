# sample-invoice

Drop your PDF/image at `input/document/<filename>` to give this example
something to extract from. The argument folder name (`document`) matches
the input declared in `workflow.yaml`.

# Evaluators run after every workflow execution that's part of an eval batch.
# Comment them in once you have an `expected/output.json` to compare against.
#
# evaluators:
#   - name: exact-match
#     type: exact-diff
#     config:
#       passThreshold: 1.0
evaluators: []

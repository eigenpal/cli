# __NAME__

A text classification workflow scaffolded by `eigenpal init workflow`. Two examples
are pre-seeded so you can immediately run `eigenpal workflow run __NAME__` and see
end-to-end behavior.

The eval config asserts on the `label` path only — confidence/rationale are
free to vary between runs without failing the eval.
